# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=

# GitHub (PAT with repo:write)
GITHUB_TOKEN=
REPO_OWNER=
REPO_NAME=
REPO_DEFAULT_BRANCH=dev

# Optional: one draft PR per run for all new articles (also: pass --batch-pr on invoke scripts)
# GITHUB_BATCH_PR=1

# AWS
AWS_REGION=
DYNAMODB_TABLE=

# Pipeline tuning
RELEVANCE_MIN_SCORE=
MAX_ARTICLES_PER_RUN=