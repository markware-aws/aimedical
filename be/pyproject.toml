[project]
name = "aimedical-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "boto3",
  "python-dotenv",
]

[tool.pyright]
pythonVersion = "3.12"
include = ["aimedical", "scripts"]
